export enum CommissionCurrency {
  AUD = 'AUD',
  USD = 'USD',
}
export enum CommissionType {
  fixed = 'fixed',
  percent = 'percent',
}

export enum CommissionPaymentType {
  prepaid = 'prepaid',
  postpaid = 'postpaid',
}
